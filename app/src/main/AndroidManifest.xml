<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jiaqi.filesmanagement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainLogin"
            android:label="@string/sign_in">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SignUp"
            android:label="@string/sign_up" />
        <activity
            android:name=".Administrator"
            android:label="档案管理" />
        <activity android:name=".Administrator2" />
        <activity
            android:name=".Management"
            android:label="管理"/>
        <activity android:name=".Add"></activity>
        <activity android:name=".Inquier_son"></activity>
    </application>

</manifest>